#include <stdio.h>

int main() {
     float w, l, area ;
     printf("Enter the value of w:");
     scanf("%f", &w);
     printf("Enter the value of l:");
     scanf("%f", &l);
     area =  w * l ;
     printf("Area of the Square :%0.2f\n ", area);
     return 0;
}
