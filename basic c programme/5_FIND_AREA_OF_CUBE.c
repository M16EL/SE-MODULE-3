#include <stdio.h>

int main() {
    float a, area;
    printf("Enter the value of a: ");
    scanf("%f", &a);
    area = 2 * a * 6;
    printf("Area of the Square: %0.2f\n", area);
    return 0;
}

