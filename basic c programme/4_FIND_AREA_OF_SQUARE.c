#include <stdio.h>

int main() {
    float a, area;
    printf("Enter the value: ");
    scanf("%f", &a);
    area = 2 * a;
    printf("Area of the Square: %0.2f\n", area);
    return 0;
}

